spring:
  application:
    name: Reporting-Service
  datasource:
    url: jdbc:postgresql://dpg-d35vr2qli9vc738ptucg-a.frankfurt-postgres.render.com/employees_db_54tp
    username: bvm
    password: gk7I712z3feW7jEUxghf4UQM4Z6otDID
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis
    redis.time-to-live: 1800000
    redis.cache-null-values: false

  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      group-id: budget-report-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      enable-auto-commit: false
      properties:
        spring.json.trusted.packages: com.budget.b.lite.dto
        max.poll.interval.ms: 300000
        session.timeout.ms: 30000
        heartbeat.interval.ms: 10000
    listener:
      ack-mode: record

server:
  port: 8083


eureka:
  client:
    service-url:
      defaultZone: http://discover:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

host:
  value: localhost:29092
  topic: finance-events
